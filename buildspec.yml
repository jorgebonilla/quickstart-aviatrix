version: 0.1
phases:
  install:
    commands:
      - echo Entered the install phase...
  build:
    commands:
      - echo Entering the build phase
      - echo Testing the scripts
      - cd scripts
      - python -m unittest test_aviatrix_poller
